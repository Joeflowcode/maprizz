/**
 * Brand colors for places that can't read CSS variables
 * (Open Graph image, theme-color meta, HTML email). Keep in sync with
 * the `@theme` block in src/app/globals.css and with src/app/icon.svg.
 */
export const brandColors = {
  ink: "#16120e",
  cream: "#f7f3ec",
  accent: "#ff8b3d",
  mist: "#bdb5a9",
} as const;
